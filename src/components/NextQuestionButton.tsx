import * as React from 'react';
import {faForward} from '@fortawesome/free-solid-svg-icons';
import {PrimaryIcon} from './PrimaryIcon';

export const NextQuestionButton = () => (
  <PrimaryIcon icon={faForward} size="3x" />
);
